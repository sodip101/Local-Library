extends layout

block content
  h1= title
  
  ul
    each author in author_list
      li 
        a(href='/catalog/'+author.url) #{author.name}
        |  #{author.lifespan}

    else
      li There are no authors.